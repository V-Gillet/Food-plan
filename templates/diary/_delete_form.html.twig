<form method="post" action="{{ path('app_meal_delete', {'meal': meal.id}) }}" onsubmit="return confirm('Cette action est définitive');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meal.id) }}">
	<button class="btn btn-quatro rounded-5 m-2 px-3 py-2 font-subTitle">Supprimer</button>
</form>
